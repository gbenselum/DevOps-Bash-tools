# Vagrant Scripts

This directory contains scripts for managing Vagrant resources.

---

### `vagrant_hosts.sh`

Generate `/etc/hosts` output from a `Vagrantfile`.

---

### `vagrant_total_mb.sh`

Calculate the RAM committed to VMs in a `Vagrantfile`.
